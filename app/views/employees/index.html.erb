<h1>Listing employees</h1>

<table border=1>
  <tr>
    <th>Name</th>
    <th>Monday</th>
    <th>Tuesday</th>
    <th>Wednesday</th>
    <th>Thursday</th>
    <th>Friday</th>
    <th>Saturday</th>
    <th>Sunday</th>
    <th>Days off</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @employees.each do |employee| %>
  <tr>
    <td><%= employee.name %></td>
    <td><%= employee.mon_start %> - <%= employee.mon_end %></td>
    <td><%= employee.tue_start %> - <%= employee.tue_end %></td>
    <td><%= employee.wed_start %> - <%= employee.wed_end %></td>
    <td><%= employee.thu_start %> - <%= employee.thu_end %></td>
    <td><%= employee.fri_start %> - <%= employee.fri_end %></td>
    <td><%= employee.sat_start %> - <%= employee.sat_end %></td>
    <td><%= employee.sun_start %> - <%= employee.sun_end %></td>
    <td><%= employee.days_off %></td>
    <td><%= link_to 'Show', employee %></td>
    <td><%= link_to 'Edit', edit_employee_path(employee) %></td>
    <td><%= link_to 'Destroy', employee, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Employee', new_employee_path %>
